import React from "react";

const Header: React.FC = () => {
  return (
    <div
      style={{
        height: "100px",
        backgroundColor: "#01194C",
        marginBottom: "2rem",
      }}
    ></div>
  );
};

export default Header;
